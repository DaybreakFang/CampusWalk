<!--miniprogram/pages/index/index.wxml-->
<navbar title="{{title}}">

  <view class="container"  >
    <view class="intro-img">
      <image src="../../images/intro.png"></image>
    </view>
    <view class="main-content">
      <view class="intro-wrap">
        <view class="intro-layout">
          <view class="banner">
            <swiper-activity list="{{swiperArr}}" />
          </view>

          <view class="card">
            <view style="flex:1">
              <card type="warning" title="扫一扫" desc="扫描校园地标附近的二维码了解更多" />
            </view>
            <view style="flex:1;margin-top:3%">
              <card type="primary" title="逛一逛" desc="游览与你位置最近10个校园地标景点" />
            </view>
          </view>
        </view>
      </view>
      <blog list="{{blogList}}" />
          <!--加载loadding-->
    <tui-loadmore wx:if="{{loadding}}" index="3" type="primary"></tui-loadmore>
<tui-nomore wx:if="{{!pullUpOn}}"></tui-nomore>
    <!--加载loadding-->
    </view>


  </view>
</navbar>